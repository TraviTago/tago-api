spring:
  datasource:
    #url: ${DATABASE_URL}
    #url: jdbc:mysql://localhost:3306/tago?serverTimezone=Asia/Seoul
    url: jdbc:mysql://tago.cjn3xrjw75zo.ap-northeast-2.rds.amazonaws.com:3306/tago?serverTimezone=Asia/Seoul
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL57Dialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    defer-datasource-initialization: false

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

jwt:
  secretKey: ${JWT_SECRET_KEY}

oauth:
  kakao:
    client-id: ${KAKAO_CLIENT_ID}
    url:
      auth: https://kauth.kakao.com
      api: https://kapi.kakao.com

slack:
  bot-token: ${SLACK_BOT_TOKEN}
  postMessageUrl : "https://slack.com/api/chat.postMessage"
  channel:
    monitor: "#불편신고"

ncloud:
  credentials:
    access-key: ${NCLOUD_ACCESS_KEY}
    secret-key: ${NCLOUD_SECRET_KEY}
  sms:
    base-url: ${NCLOUD_SMS_BASE_URL}
    service-id: ${NCLOUD_SMS_SERVICE_ID}
    calling-number: ${NCLOUD_SMS_CALLING_NUMBER}